{
    "name": "ai-chatbot-root",
    "version": "1.0.0",
    "description": "Root package to run both frontend and backend concurrently.",
    "scripts": {
        "dev": "concurrently \"npm start --prefix backend\" \"npm run dev --prefix frontend\""
    },
    "devDependencies": {
        "concurrently": "^8.2.2"
    }
}